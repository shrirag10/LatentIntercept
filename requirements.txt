# ─── Core simulation & ML ───────────────────────────────────────────────────
# Genesis physics simulator (GPU-accelerated parallel simulation)
# Install: pip install genesis-world
genesis-world

# TD-MPC2 planning agent — must be installed from source
# Install: pip install git+https://github.com/nicklashansen/tdmpc2.git
# tdmpc2  ← uncomment ONLY if the package is published to PyPI

# PyTorch (CUDA 12.x)
torch>=2.1.0
torchvision>=0.16.0

# ─── Config & Logging ───────────────────────────────────────────────────────
omegaconf>=2.3.0
hydra-core>=1.3.0
tensorboard>=2.15.0
tqdm>=4.66.0
pyyaml>=6.0.1

# ─── Array / Math utilities ─────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0

# ─── RL interface ────────────────────────────────────────────────────────────
gymnasium>=0.29.0

# ─── TensorDict / TorchRL (required by TD-MPC2 buffer) ──────────────────────
tensordict>=0.3.0
torchrl>=0.3.0

# ─── Visualisation ───────────────────────────────────────────────────────────
matplotlib>=3.8.0
